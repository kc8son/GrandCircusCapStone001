Coffeeshop app...
define a class called Product
it should have instance variables: name, category, description, and price
create a list to store all the 12 products
Create another empty list for cart(to keep track of what is ordered) and create a variable for total price
present a menu to the user by looping through all the products
allow the user to select an item by number or letter and ask for how many of that item
calculate the line total for the item (item price * quantity)
add the line total to total price
ask the user if they want to continue shopping and redisplay the menu or complete the purchase
display the subtotal of all items ordered
calculate the sales tax and add it to the subtotal which gives the grand total

ask the user for their payment type (cash, credit, or check)
if the payment type is cash, ask the user for the amount
calculate the change (amount tendered - grand total)
display the receipt with all items ordered, subtotal, grand total, and appropriate payment info including change
if the payment type is credit, ask the user for the credit card number, expiration, and CVV
display the receipt with all items ordered, subtotal, grand total, and appropriate payment info
if the payment type is check, ask the user for the check number
display a receipt with all items ordered, subtotal, grand total, and appropriate payment info.
add an option to the menu to allow the user to add a new product to the list of products by asking user for the name, category, description, and price of the new product
